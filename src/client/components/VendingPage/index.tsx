import React from "react";
import Layout from "../Layout";

type VendingProps = {};

const VendingPage = (props: VendingProps) => {
  return (
    <Layout>
      <h1>Vending</h1>
    </Layout>
  );
};

export default VendingPage;
